app:
  title: Scaffolded Backstage App
  baseUrl: http://localhost:3000

  # Enable all packages by default, this will discover packages from packages/app/package.json
  packages: all

  extensions:
    - sub-page:scaffolder/templates:
        config:
          groups:
            - title: Services
              filter:
                spec.type: service
            - title: Documentation
              filter:
                spec.type: documentation
            - title: Resources
              filter:
                spec.type: resource
    - api:home/visits: true
    - app-root-element:home/visit-listener: true
    # Default landing page configuration.
    # The catalog is mounted at /catalog by default but is overridden here to
    # serve as the root page. To switch to the Home page, comment out the
    # catalog path override and uncomment the home path override.
    - page:catalog:
        config:
          path: /
    # - page:home:
    #     config:
    #       path: /

    # Visit tracking records which pages users navigate to and is disabled by default.
    # Enable it to populate the Most Visited and Recently Visited widgets on the Home page.
    # Visit data is stored via UserSettings storage if available, otherwise browser localStorage.
    # - api:home/visits: true
    # - app-root-element:home/visit-listener: true
    - home-page-widget:home/toolkit:
        config:
          tools:
            - url: https://backstage.io/docs
              label: Docs
              icon: docs
            - url: https://github.com/backstage/backstage
              label: GitHub
              icon: github
            - url: https://github.com/backstage/backstage/blob/master/CONTRIBUTING.md
              label: Contributing
              icon: docs
            - url: https://backstage.io/plugins
              label: Plugins Directory
              icon: kind:component
            - url: https://github.com/backstage/backstage/issues/new/choose
              label: Submit New Issue
              icon: github
    - home-page-widget:home/world-clock:
        config:
          clockConfigs:
            - label: NYC
              timeZone: America/New_York
            - label: UTC
              timeZone: UTC
            - label: STO
              timeZone: Europe/Stockholm
            - label: TYO
              timeZone: Asia/Tokyo
    - page:home:
        config:
          defaultConfig:
            - component: HomePageSearchBar
              column: 0
              row: 0
              width: 12
              height: 2
              deletable: false
            - component: GettingStarted
              column: 0
              row: 1
              width: 4
              height: 11
            - component: HomePageStarredEntities
              column: 4
              row: 2
              width: 4
              height: 4
            - component: HomePageRandomJoke
              column: 8
              row: 2
              width: 4
              height: 4
            - component: HomePageToolkit
              column: 4
              row: 6
              width: 4
              height: 3
            - component: HomePageWorldClock
              column: 8
              row: 6
              width: 4
              height: 3
            - component: HomePageMostVisited
              column: 4
              row: 9
              width: 4
              height: 4
            - component: HomePageRecentlyVisited
              column: 8
              row: 9
              width: 4
              height: 4

organization:
  name: Skyworks Inc

backend:
  # Used for enabling authentication, secret is shared by all backend plugins
  # See https://backstage.io/docs/auth/service-to-service-auth for
  # information on the format
  # auth:
  #   keys:
  #     - secret: ${BACKEND_SECRET}
  baseUrl: http://localhost:7007
  listen:
    port: 7007
    # Uncomment the following host directive to bind to specific interfaces
    # host: 127.0.0.1
  csp:
    connect-src: ["'self'", 'http:', 'https:']
    # Content-Security-Policy directives follow the Helmet format: https://helmetjs.github.io/#reference
    # Default Helmet Content-Security-Policy values can be removed by setting the key to false
  cors:
    origin: http://localhost:3000
    methods: [GET, HEAD, PATCH, POST, PUT, DELETE]
    credentials: true
  # This is for local development only, it is not recommended to use this in production
  # The production database configuration is stored in app-config.production.yaml
  database:
    client: better-sqlite3
    connection:
      directory: "./db"
  # see https://backstage.io/docs/ai/mcp-actions#actions-configuration for more details
  actions:
    pluginSources:
      - auth
      - catalog
      - scaffolder
  # workingDirectory: /tmp # Use this to configure a working directory for the scaffolder, defaults to the OS temp-dir

integrations:
  github:
    - host: github.com
      # This is a Personal Access Token or PAT from GitHub. You can find out how to generate this token, and more information
      # about setting up the GitHub integration here: https://backstage.io/docs/integrations/github/locations#configuration
      token: ${GITHUB_TOKEN}
    ### Example for how to add your GitHub Enterprise instance using the API:
    # - host: ghe.example.net
    #   apiBaseUrl: https://ghe.example.net/api/v3
    #   token: ${GHE_TOKEN}

proxy:
  ### Example for how to add a proxy endpoint for the frontend.
  ### A typical reason to do this is to handle HTTPS and CORS for internal services.
  # endpoints:
  #   '/test':
  #     target: 'https://example.com'
  #     changeOrigin: true

# Reference documentation http://backstage.io/docs/features/techdocs/configuration
# Note: After experimenting with basic setup, use CI/CD to generate docs
# and an external cloud storage when deploying TechDocs for production use-case.
# https://backstage.io/docs/features/techdocs/how-to-guides#how-to-migrate-from-techdocs-basic-to-recommended-deployment-approach
techdocs:
  builder: 'local' # Alternatives - 'external'
  generator:
    runIn: 'docker' # Alternatives - 'local'
  publisher:
    type: 'local' # Alternatives - 'googleGcs' or 'awsS3'. Read documentation for using alternatives.

auth:
  # see https://backstage.io/docs/auth/ to learn about auth providers
  environment: development
  providers:
    # See https://backstage.io/docs/auth/guest/provider
    guest: {}
    github:
      development:
        clientId: Ov23li90weWjHLgnc07j
        clientSecret: fe06f51c4f6dd98dbfa2587d81261a0373e82e39
        signIn:
          resolvers:
            # Matches the GitHub username with the Backstage user entity name.
            # See https://backstage.io/docs/auth/github/provider#resolvers for more resolvers.
            - resolver: usernameMatchingUserEntityName
  # see https://backstage.io/docs/ai/mcp-actions#client-id-metadata-documents
  # to learn more about client id metadata documents
  clientIdMetadataDocuments:
    enabled: false
    # Optional: restrict which `client_id` URLs are allowed (defaults to ['*'])
    # allowedClientIdPatterns:
    #   - 'https://example.com/*'
    #   - 'https://*.trusted-domain.com/*'
    # Optional: restrict which redirect URIs are allowed (defaults to ['*'])
    # allowedRedirectUriPatterns:
    #   - 'http://localhost:*'
    #   - 'https://*.example.com/*'

scaffolder:
  # see https://backstage.io/docs/features/software-templates/configuration for software template options
  defaultAuthor:
    name: yourname # Defaults to `Scaffolder`
    email: your.email@example.com # Defaults to `scaffolder@backstage.io`
  defaultCommitMessage: "Initial commit" # Defaults to 'Initial commit'

catalog:
  import:
    entityFilename: catalog-info.yaml
    pullRequestBranchName: backstage-integration
  rules:
    - allow: [Component, System, API, Resource, Location]

  # --------------------------------------------------------------------------
  # Post-merge refresh for PR-based onboarding.
  #
  # The "PR to Existing Repository" templates (e.g.
  # examples/ai-template/agent-existing-repo.yaml) deliberately do NOT call
  # catalog:register — the catalog-info.yaml only exists on a branch at that
  # point. This provider rescans GitHub on a schedule, so once the PR is merged
  # into the default branch the new entity is registered automatically (and
  # removed again if the file is later deleted).
  #
  # `organization` is spelled out literally on purpose. It also accepts a
  # personal account (discovery queries `repositoryOwner`, which covers both
  # users and orgs). Do NOT point it at an env var: if it resolves to nothing
  # the catalog plugin throws "Either organization or app must be specified" at
  # startup and then processes NO entities at all — which looks like new
  # templates silently failing to appear rather than like a config error.
  #
  # Needs GITHUB_TOKEN with repo read scope in the shell running the backend.
  # Without it the catalog still starts and local entities still load; only the
  # scheduled GitHub scan fails, logging an error every 10 minutes.
  # Docs: https://backstage.io/docs/integrations/github/discovery
  # --------------------------------------------------------------------------
  providers:
    github:
      agentCatalogs:
        organization: 'loj-swks'
        # Matches catalog-info.yaml at the repo root and nested under e.g.
        # catalog/agents/. A wildcard path makes the provider walk each repo's
        # tree, so keep the repository filter below tight.
        catalogPath: '/**/catalog-info.yaml'
        filters:
          branch: main
          # Regex matched against the repository name. Keep this limited to
          # repos that are onboarded by pull request — it must stay in sync
          # with the repoUrl default in
          # examples/ai-template/agent-existing-repo.yaml.
          #
          # IMPORTANT: do not include repos created by the "New Repository"
          # template — those call catalog:register themselves, and letting
          # discovery claim the same catalog-info.yaml makes the catalog
          # report the entity as claimed by two locations. If you would
          # rather have discovery own everything, widen this regex AND drop
          # the catalog:register step from examples/ai-template/agent.yaml.
          repository: '^catalog-registry$'
        schedule:
          frequency: { minutes: 10 }
          timeout: { minutes: 3 }

  locations:
    # Local example data, file locations are relative to the backend process, typically `packages/backend`
    - type: file
      target: ../../examples/entities.yaml

    # Local example template
    - type: file
      target: ../../examples/ai-template/adr.yaml
      rules:
        - allow: [Template]
    - type: file
      target: ../../examples/ai-template/agent-dev.yaml
      rules:
        - allow: [Template]
    - type: file
      target: ../../examples/ai-template/agent.yaml
      rules:
        - allow: [Template]
    - type: file
      target: ../../examples/ai-template/agent-existing-repo.yaml
      rules:
        - allow: [Template]
    - type: file
      target: ../../examples/ai-template/data-pipeline.yaml
      rules:
        - allow: [Template]
    - type: file
      target: ../../examples/ai-template/llm-deployment.yaml
      rules:
        - allow: [Template]
    - type: file
      target: ../../examples/ai-template/mcp.yaml
      rules:
        - allow: [Template]
    - type: file
      target: ../../examples/ai-template/memory.yaml
      rules:
        - allow: [Template]
    - type: file
      target: ../../examples/ai-template/vector-index.yaml
      rules:
        - allow: [Template]
    - type: file
      target: ../../examples/ai-template/data-source.yaml
      rules:
        - allow: [Template]

    # Local example organizational data
    - type: file
      target: ../../examples/org.yaml
      rules:
        - allow: [User, Group]

    ## Uncomment these lines to add more example data
    # - type: url
    #   target: https://github.com/backstage/backstage/blob/master/packages/catalog-model/examples/all.yaml

    ## Uncomment these lines to add an example org
    # - type: url
    #   target: https://github.com/backstage/backstage/blob/master/packages/catalog-model/examples/acme-corp.yaml
    #   rules:
    #     - allow: [User, Group]

kubernetes:
  # see https://backstage.io/docs/features/kubernetes/configuration for kubernetes configuration options

# see https://backstage.io/docs/permissions/getting-started for more on the permission framework
permission:
  # setting this to `false` will disable permissions
  enabled: true

# see https://backstage.io/docs/ai/mcp-actions for more details
mcpActions:
  name: 'My Company Backstage' # defaults to "backstage"
  description: 'Tools for managing your software catalog, creating new services from templates, and exploring your developer portal' # optional
